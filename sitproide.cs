switch (specifier.Type)
{
    case TypeEnum.Type1:
        // Code block for type 1
        break;
    case TypeEnum.Type2:
        // Code block for type 2
        break;
    // Add more cases as needed
    default:
        // Default code block if no case matches
        break;
}
